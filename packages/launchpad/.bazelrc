common --enable_bzlmod

# rules_vite (the vitest launcher) lives in the tbzl registry;
# everything else resolves from BCR.
common --registry=https://registry.tbzl.dev/
common --registry=https://bcr.bazel.build/

build --announce_rc
build --keep_going
test --test_output=errors
