{
  "name": "meridian-web-workspace",
  "version": "0.12.0",
  "private": true,
  "description": "Workspace root for meridian-web — the web-components renderer of the meridian design language. Consumed by hosts via Bazel (//src:core) + the //bazel:panel_bundle.bzl rule. The TS surface depends on the design language via npm (@savvifi/meridian-schemas + @savvifi/meridian-proto-ts).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run --exclude tests/conformance_wasm.test.ts"
  },
  "dependencies": {
    "@bufbuild/protobuf": "2.12.1",
    "@savvifi/meridian-proto-ts": "workspace:*",
    "@savvifi/meridian-schemas": "workspace:*",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.4",
    "jsdom": "25.0.1",
    "typescript": "5.6.2",
    "vitest": "2.1.4"
  }
}
