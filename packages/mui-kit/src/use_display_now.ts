// Compatibility barrel: the hydration-safe clock belongs to the shared React
// renderer so every ComponentKit uses the same relative-label contract.
export { useDisplayNow } from "@savvifi/meridian-web-react";
